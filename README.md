# ztw-cardnews-assets

ZTW 마케팅 이슈형 카드뉴스 자동발행용 이미지 호스팅 저장소.

`scripts/instagram_cardnews.py`의 `publish_images_to_asset_repo()`가 이 저장소의 `assets/`에
카드뉴스 슬라이드 PNG를 자동 커밋·푸시하고, `raw.githubusercontent.com` URL로 인스타그램/
페이스북 Graph API에 게시한다. 이 저장소는 코드 없이 이미지 호스팅 전용이다.
